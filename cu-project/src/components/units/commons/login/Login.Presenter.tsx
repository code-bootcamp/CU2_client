import { ILoginUIProps } from "../../../../commons/types/types";
import * as S from "./Login.Style";

export default function LoginUI (props: ILoginUIProps) {
    
    return <S.Wrapper>로그인 페이지입니다.</S.Wrapper>;
}