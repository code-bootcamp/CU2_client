import { IRegisterUIProps } from "../../../../commons/types/types";
import * as S from "./Register.Style";

export default function RegisterUI (props: IRegisterUIProps) {
    
    return <S.Wrapper>회원가입 페이지입니다.</S.Wrapper>;
}