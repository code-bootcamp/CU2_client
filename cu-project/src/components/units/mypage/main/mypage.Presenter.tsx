import { IMyPageUIProps } from "../../../../commons/types/types";
import * as S from "./mypage.Style";

export default function MyPageUI (props: IMyPageUIProps) {
    
    return <S.Wrapper>마이 페이지입니다.</S.Wrapper>;
}